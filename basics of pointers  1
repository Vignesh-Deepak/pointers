int main()
{
  int a=2;
  int c,b=3;
  int *p1,*p2;
  int *p1=&a;  // address symbol is needed compulsory for all except in arrays & strings
  int *p2=&b;
  printf("%d", c=*p1+*p2);
  }
  o/p
  5
  
  
  
